//holds a word with its weight in the returned results
class WordWeight {
    Double weight;
    String word;
    
    public WordWeight(String word, Double weight) {
      this.weight = weight;
      this.word = word;
    }
}
